#ifndef __LEDS_H
#define __LEDS_H


void led_left_toggle(void);  // Used by mmlib

void led_left_on(void);   // Used by mmlib (hmi)
void led_right_on(void);  // Used by mmlib (hmi)
void led_left_off(void);  // Used by mmlib (hmi)
void led_right_off(void); // Used by mmlib (hmi)


#endif /* __LEDS_H */
